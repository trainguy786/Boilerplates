apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: glance-claim0
  name: glance-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
